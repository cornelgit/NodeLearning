// Global object
// In node we get access to the global object via different methods and properties
// In a way, it's like but different then the window object inside browser - browser's global object
// No DOM methods like browser global object - Ex. document is only in the window object

// console.log(global);

// setTimeout(() => {
//     console.log("in the timeout");
//     clearInterval(int);
// }, 3000);

// const int = setInterval(() => {
//     console.log("in the interval");
// }, 1000);

// __dirname and __filename

console.log(__dirname);
console.log(__filename);
