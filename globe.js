// Global object
// In node we get access to the global object via different methods and properties
// In a way, it's like the window object inside browser - browser's global object

// console.log(global);

setTimeout(() => {
    console.log("in the timeout");
}, 3000);
